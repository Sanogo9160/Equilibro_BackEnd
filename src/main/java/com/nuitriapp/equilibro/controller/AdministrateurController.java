package com.nuitriapp.equilibro.controller;
import com.nuitriapp.equilibro.model.Administrateur;
import com.nuitriapp.equilibro.service.AdministrateurService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
//@RequestMapping("api/administrateurs")
public class AdministrateurController {
/*
    @Autowired
    private AdministrateurService administrateurService;

    @PostMapping("/creer")
    public Administrateur creerAdministrateur(@RequestBody Administrateur administrateur) {
        return administrateurService.creerAdministrateur(administrateur);
    }

    @GetMapping("/{id}")
    public Administrateur obtenirAdministrateur(@PathVariable Long id) {
        return administrateurService.obtenirAdministrateurParId(id);
    }

    @PutMapping("modifier/{id}")
    public Administrateur modifierAdministrateur(@PathVariable Long id, @RequestBody Administrateur administrateur) {
        return administrateurService.modifierAdministrateur(id, administrateur);
    }

    @DeleteMapping("supprimer/{id}")
    public void supprimerAdministrateur(@PathVariable Long id) {
        administrateurService.supprimerAdministrateur(id);
    }

    @GetMapping("/liste")
    public List<Administrateur> obtenirTousLesAdministrateurs() {
        return administrateurService.obtenirTousLesAdministrateurs();
    }

 */
}
